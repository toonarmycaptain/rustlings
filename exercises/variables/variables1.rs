// variables1.rs
// Make me compile!
// Execute `rustlings hint variables1` or use the `hint` watch subcommand for a hint.


fn main() {
    let x: i8 = 5;  // variable must be initialised with 'let' and string inerpolation can't figure out the type of x without a type annotation. 
    println!("x has the value {}", x);
}
