// variables3.rs
// Execute `rustlings hint variables3` or use the `hint` watch subcommand for a hint.


fn main() {
    let x: i32 = 1234567890; // x must be initialised wth a value, it doesn't default to null
    println!("Number {}", x);
}
